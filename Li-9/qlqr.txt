def profissional_criar():
        for c in View.profissional_listar():
            if c.get_email() == "profissional":
                return View.profissional_inserir("profissional", "especialista", "aconselhador", "profissional", "54321")

def profissional_autenticar(email, senha):
        for c in View.profissional_listar():
            if c.get_email() == email and c.get_senha() == senha:
                return{"id": c.get_id(), "nome": c.get_nome()}
        return None

        conselho = st.text_input("Informe o novo conselho", op.get_conselho())